Drop your song here, named exactly:

    our-song.mp3

so the file path is public/music/our-song.mp3

If you use a different filename, update `src/data/story.ts` -> musicConfig.src.
Until a file exists here, the site still loads fine — the music button
just shows as disabled.
